extends layouts/main.pug

block content
    .form-container
        form(action=routes.join, method="post")
            input(type="text", name="name", plaeholder="Full Name")
            input(type="email", name="email", plaeholder="Email")
            input(type="password", name="password", plaeholder="Password")
            input(type="password", name="password2", plaeholder="Verify Password")
            input(type="submit", value="Join Now")
        include partials/socialLogin